version: '3.8'

services:
  git-analyzer:
    build: .
    ports:
      - "3000:3000"
    environment:
      - GH_TOKEN=${GH_TOKEN}
      - GROQ_API_KEY=${GROQ_API_KEY}
      - NODE_ENV=production
    restart: unless-stopped
